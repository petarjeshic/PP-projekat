program P
	const int size = 10;
	class Table {
		int pos[], neg[];
		{
 			void putp (int a, int idx) { pos[idx]=a; }
 			void putn (int a, int idx) { neg[idx]=a; }
 			int getp (int idx) { return pos[idx]; }
 			int getn (int idx) { return neg[idx]; }
		}
	}
	Table val;
	{
		void f(char ch, int a, int ... vargs)
		int x;
		{
			x = vargs[1];
		}
		void main()
		int x, i;
		char c;
		{ //‐‐‐‐‐‐‐‐‐‐ Initialize val
			val = new Table;
			val.pos = new int [size];
			val.neg = new int [size];
			for (i = 0; i<size; i++)
			{
				val.putp(0,i);
 				val.putn(0,i);
			}
			f(c, x,i,size);
			//‐‐‐‐‐‐‐‐‐‐ Read values
			read(x);
			for (;x > 0;)
			{
				if (0 <= x && x < size)
				{
					val.putp(val.getp(x)+1);
				} else
 				if (‐size < x && x < 0)
 				{
					val.putn(val.getn(‐x)+1);
 				}
				read(x);

			}